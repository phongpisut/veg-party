# Nostr room / channel tag for this build. Each deployed environment can use a
# different value to get its own isolated room (resets all shared data).
# Optional — defaults to 'duck-river-party-v2' when unset.
#
# VITE_GAME_TAG=duck-river-party-v2